extends TileMap

export(String) var stage = "level1"

func _ready():
#	var file = File.new()
#	file.open("res://level1.json", File.WRITE)
#	file.store_string(str(get_used_cells()))
#	file.close()
	pass
